import { StringMap } from "@angular/compiler/src/compiler_facade_interface";

export interface Gift{
    id: number,
    name: string,
    description: string,
    linkUrl: string,
    imageUrl: string,
    giftCode: string
}